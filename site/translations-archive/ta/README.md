Tamil (ta) translation — archived

This is the full Tamil translation of the Curiosity Cat landing page, removed
from the active language switcher and site build in July 2026. Nothing here
was deleted — it's preserved intact for a possible future deployment on a
separate site.

It is not linked from the live site and does not ship. `index.html`'s
relative asset paths (`../site/css/...`, `../site/js/...`) assumed the old
location at the repository root (`ta/`) and will need to be updated to
wherever this is redeployed.
