# Apple Developer Program — enrolled 22 Jul 2026
Team ID: R42NHWRJL6
Program: Apple Developer Program, enrolled as Individual (Mark Cleary).

This unblocks signing + notarisation (see docs/app/SIGNING.md). Team ID is not a secret. Still needed from Mark, bundle into ONE sitting when the build is ready: (1) create + install the Developer ID Application certificate on this machine (his Apple account login), (2) mint an app-specific password or App Store Connect API key for notarytool, stored in Keychain — never in files. Prepare everything up to those two steps first so his sitting is minutes, not an afternoon.
