{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for all Curiosity Cat windows — core commands, permission to spawn the ccat-engine sidecar, and the GitHub Releases updater.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "updater:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/ccat-engine",
          "sidecar": true
        }
      ]
    }
  ]
}
